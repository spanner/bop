Description:
    Creates a droom initializer and copies across the necessary migrations.

Example:
    rails generate bop:install

    This will create:
        config/initializers/droom.rb
        several migration files ending in .droom.rb
